<% provide :pre_title, t("c-summary.tag.rename") + ' | ' + t("console") %>
<% provide :content_title, t("c-summary.tag.rename") %>
<% provide :form_url, admin_tag_path(@tag.id) %>

<%= render 'new_or_edit' %>
